finish listings so that all listings show with images
add googlemaps + markers for listings
add location filter for listings
add listing filters form
style listing page
add listing detail page
style detail page
